# Overview — End-to-End Architecture

## System Architecture

```
┌─────────────┐     HTTP/JSON      ┌──────────────────┐
│  opencode/pi │  ──────────────►   │  Dragon Q6A      │
│  (client)    │  ◄──────────────   │  :8080            │
└─────────────┘                     │  genie-rs (Rust)  │
                                    │  Genie SDK (C)    │
                                    │  QAIRT 2.47       │
                                    │  QNN HTP backend  │
                                    │  │                │
                                    │  ├── libQnnHtp.so──┤──── fastrpc ────► Hexagon v68 NPU
                                    │  ├── libQnnHtpV68Skel.so (DSP binary)
                                    │  ├── libGenie.so
                                    │  └── model context binary (1.7GB)
                                    └──────────────────┘
```

## Component Details

### 1. NPU DMA Fix (Kernel Patch)

The Qualcomm fastrpc driver (which mediates communication with the Hexagon DSP) requires a reserved DMA memory pool in the Device Tree. Stock kernel 6.18.x ships without this pool, causing:

```
no reserved DMA memory for FASTRPC
Error 14001 (AEE_EUNSUPPORTED)
```

**Fix**: Patch the Device Tree Blob (DTB) on the EFI System Partition to add:
- A `shared-dma-pool` at `0x89000000` (32 MB)
- `memory-region` property on both CDSP (`remoteproc@a300000`) and ADSP (`remoteproc@3700000`) fastrpc nodes

See `docs/02-npu-dma-fix.md` for the full procedure.

### 2. Model

- **Model**: Llama 3.2 1B (text generation, instruction-tuned)
- **Format**: QNN HTP context binary (`.serialized.bin`) — pre-compiled for Hexagon v68
- **Source**: [ModelScope — radxa/Llama3.2-1B-1024-qairt-v68](https://modelscope.cn/models/radxa/Llama3.2-1B-1024-qairt-v68)
- **Size**: 1.7 GB (INT8 quantized, 1024 context window)
- Includes: `libQnnHtp.so`, `libQnnHtpV68Stub.so`, `libQnnHtpV68Skel.so`, `libGenie.so`, tokenizer, configs

### 3. genie-rs API Server

The API server is built in Rust using:
- **axum** — HTTP framework (OpenAI-compatible routes)
- **Genie SDK C bindings** — generated by bindgen from QAIRT headers
  - `GenieDialogConfig_createFromJson()` — loads HTP config
  - `GenieDialog_create()` — creates inference context
  - `GenieDialog_query()` — runs inference with streaming callback
- **HTP backend** — `QnnHtp` type with `QnnHtpV68Stub`/`Skel`
- **`--working-dir`** — enables running from the model directory (required for Skel loading)

### 4. Boot Process

The Dragon Q6A boots via **UEFI + systemd-boot** (not U-Boot):

```
UEFI → systemd-boot → kernel + initrd + DTB from ESP
```

- DTB: `/boot/efi/RadxaOS/<kernel-version>/qcs6490-radxa-dragon-q6a.dtb`
- Kernel: `/boot/efi/RadxaOS/<kernel-version>/linux`
- Boot entry: `/boot/efi/loader/entries/RadxaOS-<kernel-version>.conf`

### 5. Memory Map

```
0x82700000  cdsp-secure-heap@82700000    (CDSP secure memory)
0x82710000  ── end of cdsp-secure-heap
    ...
0x89000000  fastrpc@89000000  (32 MB)    ← NEW: shared-dma-pool for fastrpc
0x8b000000  ── end of pool
    ...
0x8b800000  adsp@8b800000                 (ADSP region)
```

The 32 MB pool sits in a ~144 MB gap between CDSP and ADSP regions.

## Key Constraints

| Constraint | Detail |
|------------|--------|
| **CWD must be model dir** | `libQnnHtp.so` reads `libQnnHtpV68Skel.so` from CWD (not LD_LIBRARY_PATH) |
| **Context window** | 1024 tokens — limited by the pre-compiled context binary |
| **Memory** | ~3.5 GB used during inference (1.7 GB model + 32 MB DMA pool + framework) |
| **NPU only** | This setup uses QNN HTP (Hexagon), not CPU. GenAiTransformer is a fallback |
| **1B model limit** | No larger v68 context binaries exist. 3B+ would need CPU GenAiTransformer |

## File Layout on Dragon

```
/home/daniel/
├── llama-v68-model/              # ModelScope model directory
│   ├── models/
│   │   └── weight_sharing_model_1_of_1.serialized.bin  (1.7 GB)
│   ├── tokenizer.json
│   ├── htp-model-config-llama32-1b-gqa.json
│   ├── htp_backend_ext_config.json
│   ├── configuration.json
│   ├── libQnnHtp.so
│   ├── libQnnHtpV68Stub.so
│   ├── libQnnHtpV68Skel.so       (8.3 MB — Hexagon DSP binary)
│   ├── libQnnSystem.so
│   ├── libQnnHtpNetRunExtensions.so
│   └── genie-t2t-run             (standalone test binary)
│
└── source/dragon-ai/              # genie-rs project
    ├── src/
    │   ├── main.rs
    │   ├── config/
    │   │   ├── mod.rs
    │   │   ├── registry.rs
    │   │   └── qnn_config.rs
    │   ├── context/genie_context.rs
    │   ├── server/
    │   │   ├── mod.rs
    │   │   └── routes.rs
    │   └── ffi/mod.rs
    ├── models/registry.toml
    ├── build.rs
    └── Cargo.toml
```
